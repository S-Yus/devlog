# コンテンツ基準

記事を書き始める前に，GuideかDevlogかを決めます。Projectsは記事ではなく制作物の紹介に使います。

## Guide

他の人が同じ作業を再現するための記事です。

### 扱う内容

- インストールと環境構築
- 初期設定と各種セッティング
- 特定の作業を完了するための手順
- エラーの原因調査と解決方法
- 動作確認済みの技術解説
- 比較検証から得た結論

### 掲載条件

- 何ができる記事なのかを最初に書く
- OS，バージョン，前提条件を書く
- コマンドを実際に実行している
- 期待する結果を書く
- 発生したエラーは原文を残す
- トラブルシューティングに原因と解決方法を書く
- 破壊的な操作には元に戻す方法を書く
- 制約や未確認部分を隠さない
- 公開前に最初から手順をやり直す

検証が終わるまでは次の状態にします。

```yaml
status: draft
draft: true
```

公開時は次の状態にします。

```yaml
status: verified
draft: false
```

環境変化などで再確認が必要になった場合：

```yaml
status: needs-update
```

現在は使用を勧められない場合：

```yaml
status: deprecated
```

### categoryの目安

| category | 内容 |
| --- | --- |
| `setup` | 導入，インストール，環境構築 |
| `how-to` | 特定の作業を完了する手順 |
| `troubleshooting` | エラー調査と解決 |
| `development` | 実装，開発環境，ビルド |
| `reference` | 仕様，コマンド，設定値の整理 |

複数に当てはまる場合は，記事の主目的を1つ選びます。

## Devlog

作業中の事実をその時点のまま残す記事です。再現性や結論は必須ではありません。

### 扱う内容

- 今日試したこと
- 失敗した方法
- 調査途中のエラー
- 仮説
- 技術選定や判断の変化
- 次に試すこと
- Guideにする前の検証記録

公開できる途中経過なら，`status`が`ongoing`や`blocked`でも`draft: false`にできます。個人情報，認証情報，未公開情報が含まれていないことは公開前に確認します。

検証が完了し，他の人が再現できる内容になったら，別途Guideとしてまとめます。元のDevlogには完成したGuideへのリンクを追加します。

## Projects

完成した制作物や継続中のプロジェクトを紹介する場所です。

- 何を作ったか
- なぜ作ったか
- 自分の担当範囲
- 使用技術
- 設計上の判断
- 現在の状態
- 公開URLやソースコード
- 関連するGuideとDevlog

操作手順そのものはGuide，制作中の経過はDevlogへ分けます。

## 判断に迷ったとき

```text
読者が同じ結果を再現するための記事か？
├─ はい → Guide
└─ いいえ
   ├─ 作業途中の記録か？ → Devlog
   └─ 作ったものの紹介か？ → Projects
```

Guideとして書き始めても，検証が終わらなければDevlogへ移します。Devlogとして始めた内容が固まったらGuideへ書き直します。

## 公開前チェック

- `draft: false`へ変更した
- 日付とステータスが正しい
- 秘密情報が含まれていない
- 内部リンクと外部リンクを開ける
- コマンドと手順を確認した
- `npm run build`が成功する
- 表示と検索結果を確認した
