# コンテンツ基準

記事を書き始める前に，Guide，Devlog，Readingのどれに入れるかを決めます。Projectsは記事ではなく制作物の紹介に使います。

## Guide

他の人が同じ作業を再現するための記事です。

### 扱う内容

- インストールと環境構築
- 初期設定と各種セッティング
- 特定の作業を完了するための手順
- エラーの原因調査と解決方法
- 動作確認済みの技術解説
- 比較検証から得た結論

### 掲載条件

- 何ができる記事なのかを最初に書く
- OS，バージョン，前提条件を書く
- コマンドを実際に実行している
- 期待する結果を書く
- 発生したエラーは原文を残す
- トラブルシューティングに原因と解決方法を書く
- 破壊的な操作には元に戻す方法を書く
- 制約や未確認部分を隠さない
- 公開前に最初から手順をやり直す

検証が終わるまでは次の状態にします。

```yaml
status: draft
draft: true
```

公開時は次の状態にします。

```yaml
status: verified
draft: false
```

環境変化などで再確認が必要になった場合：

```yaml
status: needs-update
```

現在は使用を勧められない場合：

```yaml
status: deprecated
```

### categoryの目安

| category | 内容 |
| --- | --- |
| `setup` | 導入，インストール，環境構築 |
| `how-to` | 特定の作業を完了する手順 |
| `troubleshooting` | エラー調査と解決 |
| `development` | 実装，開発環境，ビルド |
| `reference` | 仕様，コマンド，設定値の整理 |

複数に当てはまる場合は，記事の主目的を1つ選びます。

## 更新履歴

Guide，Devlog，Readingは，Frontmatterの`history`で公開・更新履歴を管理します。本文に`更新履歴`見出しを手書きする必要はありません。

```yaml
history:
  - at: "2026-08-08T12:00:00+09:00"
    summary: "初版公開"
  - at: "2026-08-09T18:30:00+09:00"
    summary: "検証環境と実行結果を追加"
```

- 古い履歴から順に並べる
- `at`は`YYYY-MM-DDTHH:mm:ss+09:00`形式で記録する
- `summary`には読者が分かる変更内容を書く
- 公開時に最初の`初版公開`を追加してから`draft: false`にする
- 記事を更新して公開するたびに1件追加する
- 下書き中は`history: []`でよい

記事末尾の表示，ヘッダーの公開・更新日時，検索エンジン向けの公開・更新日時はこの履歴から自動生成されます。

## テンプレートの関連リンク

GuideとDevlogの新規テンプレートは，本文に小見出しを自動生成せず，末尾の`関連リンク`だけを用意します。Readingは読書メモ用の見出しと`関連リンク`を用意します。不要な見出しは削除してかまいません。

`関連リンク`には次の4リンクを必ず含めます。

- [Guide一覧](/guides/)
- [Devlog一覧](/devlog/)
- [Reading一覧](/reading/)
- [Projects](/projects/)

## Devlog

Devlogには日誌と作業記録があります。再現性や結論は必須ではありません。

### 日誌（`kind: journal`）

一日を主語にして，その日に取り組んだことや考えたことを残します。複数の小さな話題を含められます。

- ファイル名は`YYYY-MM-DD.md`または`YYYY-MM-DD-topic.md`
- `project`と`status`は使用しない
- 詳細な技術検証は作業記録へ分けてリンクする

```yaml
kind: journal
publishedAt: 2026-08-07
tags:
  - Daily
history:
  - at: "2026-08-07T22:00:00+09:00"
    summary: "初版公開"
draft: false
```

作成コマンド：

```bash
npm run new:journal
```

### 作業記録（`kind: worklog`）

特定の実装，問題，検証を主語にして，複数日にわたる途中経過や判断を残します。

- ファイル名は`bulletin-board-auth.md`のように内容を表すslug
- `project`と`status`を必ず指定する
- 手順が再現可能な状態まで固まったらGuideへまとめる

```yaml
kind: worklog
project: bulletin-board
status: ongoing
history:
  - at: "2026-08-07T22:00:00+09:00"
    summary: "初版公開"
draft: false
```

作成コマンド：

```bash
npm run new:devlog -- bulletin-board-auth
```

### 判断基準

| 質問 | 種類 |
| --- | --- |
| 「今日は何をしたか」が中心 | 日誌 |
| 「この問題をどう調査・実装したか」が中心 | 作業記録 |
| 他の人が同じ結果を再現できる | Guide |

### 扱う内容

- 今日試したこと
- 失敗した方法
- 調査途中のエラー
- 仮説
- 技術選定や判断の変化
- 次に試すこと
- Guideにする前の検証記録

作業記録は，`status`が`ongoing`や`blocked`でも公開できる途中経過なら`draft: false`にできます。個人情報，認証情報，未公開情報が含まれていないことは公開前に確認します。

検証が完了し，他の人が再現できる内容になったら，別途Guideとしてまとめます。元のDevlogには完成したGuideへのリンクを追加します。

## Reading

読んだ論文，技術記事，公式資料の要点，疑問，自分の解釈を残す場所です。手順を再現させる記事はGuide，自分で行った開発・検証の経過はDevlogに分けます。

### 資料種別

| `sourceType` | 内容 |
| --- | --- |
| `paper` | 論文，プレプリント |
| `article` | 技術記事，ブログ，解説 |
| `documentation` | 公式ドキュメント，仕様書 |

### ざっくり読み

まず要点と価値を判断したい資料に使います。要点，問題設定，核心となるアイデア，主要結果，自分の解釈，疑問だけを記録します。

```bash
npm run new:reading -- paper-slug
```

### がっつり読み

今後の研究や実装で参照する重要な論文に使います。前提，既存手法，提案手法，実験条件，アブレーション，限界，関連論文，読書履歴まで整理します。

```bash
npm run new:reading:deep -- paper-slug
```

### 読書状態

| `status` | 使う場面 |
| --- | --- |
| `unread` | 未読。読む予定だけ登録した |
| `skimmed` | ざっくり読みを終えた |
| `read` | 全体を読み終えた |
| `deep-read` | 手法や実験結果まで精読した |

```yaml
status: skimmed
```

`readAt: `は読書中なら空欄のままにします。読み終えたら読了日を入れます。

```yaml
readAt: 2026-08-10
```

本文の`読書履歴`には読んだ日，範囲，理解できたこと，次に確認することを追記します。Frontmatterの`history`は読書履歴ではなく，サイト上での公開・更新履歴です。

## Projects

完成した制作物や継続中のプロジェクトを紹介する場所です。

- 何を作ったか
- なぜ作ったか
- 自分の担当範囲
- 使用技術
- 設計上の判断
- 現在の状態
- 公開URLやソースコード
- 関連するGuideとDevlog

操作手順そのものはGuide，制作中の経過はDevlogへ分けます。

## 判断に迷ったとき

```text
読者が同じ結果を再現するための記事か？
├─ はい → Guide
└─ いいえ
   ├─ 読んだ資料の要点や考察か？ → Reading
   ├─ 一日を単位にした記録か？ → Devlog / 日誌
   ├─ 特定の作業や問題の記録か？ → Devlog / 作業記録
   └─ 作ったものの紹介か？ → Projects
```

Guideとして書き始めても，検証が終わらなければDevlogへ移します。Devlogとして始めた内容が固まったらGuideへ書き直します。

## 公開前チェック

- `draft: false`へ変更した
- 日付と記事種別が正しい
- DevlogとReadingでは`status`が内容と一致している
- Readingを読み終えた場合は`readAt`を入れた
- 秘密情報が含まれていない
- 内部リンクと外部リンクを開ける
- コマンドと手順を確認した
- `npm run build`が成功する
- 表示と検索結果を確認した
